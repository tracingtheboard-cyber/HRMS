# 快速部署清单

## 第一步：准备代码

1. **确保所有更改已提交**
   ```bash
   git add .
   git commit -m "准备部署"
   git push
   ```

2. **生成 APP_KEY**（如果还没有）
   ```bash
   php artisan key:generate
   ```
   复制生成的密钥值，部署时需要用到。

## 第二步：选择部署平台

### 🚀 Render.com（最简单，推荐）

1. 访问 https://render.com，使用 GitHub 登录
2. 点击 "New +" → "Web Service"
3. 连接你的 GitHub 仓库
4. 配置：
   - **Name:** hrms
   - **Region:** Singapore (或离你最近)
   - **Branch:** main (或你的主分支)
   - **Root Directory:** (留空)
   - **Environment:** PHP
   - **Build Command:** `composer install --no-dev --optimize-autoloader`
   - **Start Command:** `php artisan serve --host=0.0.0.0 --port=$PORT`
   - **Plan:** Free

5. **添加 MySQL 数据库**
   - 点击 "New +" → "PostgreSQL" (Render 免费套餐提供 PostgreSQL)
   - 或者使用外部 MySQL 服务（如 PlanetScale 免费版）

6. **配置环境变量**
   在 Web Service 的 "Environment" 标签页添加：

   ```
   APP_NAME=HRMS
   APP_ENV=production
   APP_KEY=你的APP_KEY
   APP_DEBUG=false
   APP_URL=https://你的应用名.onrender.com
   
   DB_CONNECTION=pgsql  # 如果用 PostgreSQL
   # 或 DB_CONNECTION=mysql  # 如果用 MySQL
   DB_HOST=数据库主机
   DB_PORT=5432  # PostgreSQL 默认
   DB_DATABASE=数据库名
   DB_USERNAME=数据库用户名
   DB_PASSWORD=数据库密码
   
   MAIL_MAILER=log  # 测试阶段用 log，不会真的发邮件
   ```

7. **部署后运行迁移**
   - 部署完成后，点击 "Shell" 标签页
   - 运行：
     ```bash
     php artisan migrate --force
     php artisan storage:link
     php artisan db:seed --class=TestUserSeeder
     php artisan db:seed --class=TestEmployeeDataSeeder
     ```

### 🚂 Railway.app（也很快）

1. 访问 https://railway.app，使用 GitHub 登录
2. "New Project" → "Deploy from GitHub repo"
3. 选择你的仓库
4. 添加 MySQL 插件
5. 配置环境变量（同上）
6. 自动部署

## 第三步：获取访问链接

部署完成后，你会得到一个类似这样的链接：
- Render: `https://hrms-xxxx.onrender.com`
- Railway: `https://hrms-production.up.railway.app`

把这个链接分享给朋友即可测试！

## 测试账号

系统会自动创建以下测试账号：

- **管理员：**
  - 邮箱: admin@example.com
  - 密码: password

- **HR：**
  - 邮箱: hr@example.com
  - 密码: password

- **员工：**
  - 邮箱: employee@example.com
  - 密码: password

## 注意事项

1. **首次部署可能需要 5-10 分钟**
2. **免费版有冷启动时间**（Render 免费版15分钟无访问会休眠）
3. **文件上传功能**需要确保 `storage/app/public` 目录存在且有写入权限
4. **如果遇到错误**，查看平台的日志输出

## 常见问题

**Q: 数据库连接失败？**
A: 检查环境变量中的数据库配置是否正确，特别是主机地址和端口。

**Q: 500 错误？**
A: 检查日志：`php artisan log:clear` 然后重现错误，查看 `storage/logs/laravel.log`

**Q: 静态资源404？**
A: 运行 `php artisan storage:link`，确保 public/storage 链接存在。

**Q: 中文显示乱码？**
A: 确保数据库字符集是 utf8mb4，在 .env 中设置正确的字符集。


